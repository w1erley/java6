package equipment;

/** Похідні типи (для різних класів, щоб equals враховував тип). */
public class Shield extends Equipment {
    public Shield(String n, int w, int p) {
        super(n, w, p);
    }
}