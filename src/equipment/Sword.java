package equipment;

/** Похідні типи (для різних класів, щоб equals враховував тип). */
public class Sword extends Equipment {
    public Sword(String n, int w, int p) {
        super(n, w, p);
    }
}